import blockContent from './blockContent';
import category from './category';
import tag from './tag';
import post from './post';

export const schemaTypes = [post, category, tag, blockContent];

export default schemaTypes;
